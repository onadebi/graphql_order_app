import { Address } from "./address";


export type CustomerType = {
    uid: string | number;
    email: string;
    name: string;
    phone: string;
    // address: Address;
    // createdAt: number;
    // updatedAt: number;
};